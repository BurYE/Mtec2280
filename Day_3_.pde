
  //draw the menu
  background(0);
  fill(255,0,0);
  noStroke();
  //float "variable" = "value" it's not that hard if you know it
  float x = .1 * width;
  float y = .2 * height;
  float w = .25 * width;
  float z = .5 * height;
  
  //Float is used to make a variable. So the code won't look stupid. Or actually more stupid
  //cause if you're new to this you'd be like what tf are these letters for???
  rect(x,y,w,z);
  //this is used^^ for proportions, I guess it's conveinient when you don't wanna do the math for the exact location
  
